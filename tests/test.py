from carmac_tools import match_names_to_filenames

# matches a list of names against a list of files and outputs the resulting list of filenames matched to a txt file.
# match_names_to_filenames('dat_retool_short', 'database_zip_filelist_short.txt',
#                          'rapidfuzz', 'exact', 80, r'[^a-z0-9]+',
#                          sort=True, verbose=True, output_to_file=True)

# match_names_to_filenames('dat_retool', 'database_zip_filelist.txt',
#                          'rapidfuzz', 'exact', 80, r'[^a-z0-9]+',
#                          sort=True, verbose=True, output_to_file=True)

# match_names_to_filenames('gg_fav.txt', 'matched_list_.txt',
#                          'rapidfuzz', 'partial', 60, r'[^a-z0-9]+',
#                          sort=True, verbose=True, output_to_file=True)



